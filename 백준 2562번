#include <stdio.h>

int main() 
{
	int a[9] = {0};
	int b;
	int max = 0;

	for (int i = 0;i < 9;i++) 
	{
		scanf("%d", &a[i]);
		if (a[i] > max) {
			max = a[i];
			b = i;
		}
	}
	
	printf("%d\n", max);
	printf("%d", b+1);
}
